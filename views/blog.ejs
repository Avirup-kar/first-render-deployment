<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <Style>
        body {
            background: #181a1b;
            color: #e0e0e0;
            padding: 0px 0;
            margin: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
            min-height: 100vh;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            background: #23272a;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.25);
            overflow: hidden;
        }
        .img-container {
            width: 100%;
            height: 340px;
            overflow: hidden;
            background: #181a1b;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .img-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 0 0 12px 12px;
            transition: transform 0.3s, filter 0.3s;
            filter: brightness(0.92) contrast(1.08);
        }
        .img-container img:hover {
            transform: scale(0.95);
            filter: brightness(1) contrast(1.15);
        }
        .top {
            background: linear-gradient(90deg, #23272a 80%, #2c2f34 100%);
            padding: 16px;
            width: 91%;
            margin: auto;
            border-radius: 12px 12px  0 0;
            display: flex;
            justify-items: center;
            justify-content: space-between;
            gap: 6px;
        }
        h2 {
            margin: 0;
            color: #d0950b;
            font-size: 2.2rem;
            letter-spacing: 1px;
            font-weight: 700;
            background: transparent;
        }
        .author {
            color: #bdbdbd;
            font-size: 1rem;
            margin-bottom: 0;
        }

        .top img {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 10px;
            border: 2px solid #d0950b;
            box-shadow: 0 2px 8px rgba(0,0,0,0.18);
            vertical-align: middle;
        }


        .beforeLast {
            display: flex;
            align-items: center;
            padding: 10px;
            background: #2c2f34;
            border-radius: 8px;
            margin-bottom: 10px;
        }

        .beforeLast img{
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 10px;
            border: 2px solid #d0950b;
            box-shadow: 0 2px 8px rgba(0,0,0,0.18);
            vertical-align: middle;
        }


        .last {
            width: 90%;
            margin: 0 auto 32px auto;
            padding: 28px 24px;
            min-height: 60px;
            background: #23272a;
            color: #e0e0e0;
            border-radius: 0 0 12px 12px;
            font-size: 1.15rem;
            line-height: 1.7;
            box-shadow: 0 2px 8px rgba(0,0,0,0.10);
        }
        p {
            color: #bdbdbd;
        }
        @media (max-width: 900px) {
            .container {
            max-width: 98vw;
            }
            .img-container {
            height: 220px;
            }
            .last {
            width: 98%;
            padding: 18px 8px;
            }
            .top {
            padding: 18px 10px 10px 10px;
            }
        }

        .signup-container {
            max-width: 620px;
            margin: 60px auto;
            padding: 36px;
            background: #23272b;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.25);
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .signup-container h2 {
            margin-bottom: 24px;
            color: #f1f1f1;
        }
        .signup-container form {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .signup-container form > div {
            margin-bottom: 18px;
            width: 100%;
        }
        .signup-container label {
            display: block;
            margin-bottom: 7px;
            color: #bdbdbd;
            font-weight: 500;
        }
        .signup-container input {
            width: 100%;
            padding: 12px 14px;
            border: 1px solid #444;
            border-radius: 7px;
            font-size: 1.08rem;
            background: #181a1b;
            color: #f1f1f1;
            outline: none;
            transition: border 0.2s;
            box-sizing: border-box;
        }
        .signup-container input:focus {
            border: 1.5px solid #007bff;
        }
        .signup-container button {
            width: 100%;
            padding: 12px 0;
            background: #007bff;
            color: #fff;
            border: none;
            border-radius: 7px;
            font-size: 1.08rem;
            cursor: pointer;
            transition: background 0.2s;
        }
        .signup-container button:hover {
            background: #0056b3;
        }
    </Style>
</head>
<body>
    <div style="max-width: 800px; margin: 40px auto; box-shadow: 0 2px 8px rgba(0,0,0,0.1); border-radius: 8px; overflow: hidden; font-family: Arial, sans-serif;">
        <% if (blog.coverpic) { %>
            <div class="img-container" style="width: 100%; height: 420px; overflow: hidden; background: #111;">
                <img src="<%= blog.coverpic %>" alt="Cover Image" style="width: 120%; height: 120%; object-fit: cover;">
            </div>
            <% } %>
        </div>
        <div class="top" style="background: #a8a7a7">
            <h2 style="margin: 0;"><%= blog.title %></h2>
            <div style="display: flex; align-items: center; gap: 3px;">
                <img src="<%= blog.createdtBy.profileImageUrl %>" alt="profile pic">
                <span style="color: #fffdfd; font-size: 20px;">By: <%= blog.createdtBy.fullname %></span>
            </div>
        </div>

    <div style=" background-color: transparent;">
        <p class="last"><%= blog.content %></p>
    </div>

    <div class="signup-container">
        <h2>Coment</h2>
        <% if (locals.user) { %>
           <form action="/blog/coments/<%= blog._id %>" method="POST">
            <div>
                <input type="text" name="content" required>
            </div>
            <button type="submit">ADD</button>
        </form>
        <% } %>
    </div>

    <div>
        <% if (coments.length > 0) { %>
            <h3 style="color: #f1f1f1; text-align: center;">Coments(<%= coments.length %>)</h3>
            <% coments.forEach(coment => { %>
                <div class="beforeLast" style="margin: 10px; padding: 10px; background: #2c2f34; border-radius: 8px;">
                    <img src="<%= coment.createdtBy.profileImageUrl %>" alt="">
                    <p style="margin: 0; color: #bdbdbd;"><strong><%= coment.createdtBy.fullname %>:</strong> <%= coment.content %></p>
                </div>
            <% }) %>
        <% } else { %>
            <p style="text-align: center; color: #bdbdbd;">No coments yet.</p>
        <% } %>
    </div>
</body>
</html>